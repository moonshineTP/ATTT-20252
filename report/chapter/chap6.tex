% ============================================================
%  Chương VI – Các hệ thống PKI mã nguồn mở
%  ~700 từ · 2 trang
% ============================================================
\chapter{Các hệ thống PKI mã nguồn mở}
\label{chap:opensource}

% Ý chính: PKI không chỉ là lý thuyết — có nhiều tool mã nguồn mở cho phép
% xây dựng PKI từ môi trường lab nhỏ đến doanh nghiệp lớn.

% ----------------------------------------------------------
\section{OpenSSL}
\label{sec:6.1}

\subsubsection*{Tổng quan OpenSSL}
% Ý chính: thư viện mật mã nền tảng phổ biến nhất (Apache, Nginx, OpenVPN, curl,...)
% Hai thành phần: libssl (TLS handshake) + libcrypto (mật mã tổng quát)
% Heartbleed 2014: bài học về dependency vào một thư viện duy nhất

\subsubsection*{Xây dựng CA đơn giản với OpenSSL}
% Ý chính: demo workflow hoàn chỉnh:
%   1. genrsa / ecparam → tạo khóa Root CA
%   2. req -x509 → self-signed Root CA cert
%   3. req → tạo CSR cho server
%   4. x509 -CA → ký cert bằng Root CA
%   5. verify → kiểm tra chain
% Listing~\ref{lst:openssl-ca}: các lệnh terminal minh họa

% ----------------------------------------------------------
\section{EJBCA}
\label{sec:6.2}
% Ý chính: PKI doanh nghiệp đầy đủ, viết Java/JEE, mã nguồn mở (LGPL)
% Tính năng: Multi-CA, HSM, RA portal web, OCSP Responder, CMP, SCEP, EST
% Triển khai nhanh qua Docker; bản Community miễn phí
% Phù hợp: tổ chức tài chính, nhà nước, telco cần PKI quy mô lớn

% ----------------------------------------------------------
\section{Let's Encrypt và Certbot}
\label{sec:6.3}

\subsubsection*{Let's Encrypt và sứ mệnh}
% Ý chính: CA phi lợi nhuận (ISRG); phát hành >3 tỷ cert từ 2015
% Mục tiêu: "Encrypt the entire Web" — từ <40\% HTTPS lên >94\% (Chrome 2023)
% Cert miễn phí, tự động, chỉ DV \cite{RFC8555}

\subsubsection*{ACME Protocol và Certbot}
% Ý chính: ACME (Automated Certificate Management Environment) tự động hoá DV challenge
% Challenge types: HTTP-01 (file trên domain), DNS-01 (TXT record), TLS-ALPN-01
% Certbot: client phổ biến nhất, hỗ trợ --auto-renew (cron)
% Giới hạn: chỉ DV, không offline use-case, rate limits (50 cert/domain/tuần)

% ----------------------------------------------------------
\section{Các công cụ và nền tảng khác}
\label{sec:6.4}

\subsubsection*{XCA}
% Ý chính: GUI tool đa nền tảng, phù hợp học tập và lab;
% không cần CLI — kéo thả để tạo CA hierarchy, xuất PKCS\#12

\subsubsection*{HashiCorp Vault PKI Secrets Engine}
% Ý chính: cloud-native PKI; phát hành cert short-lived (5–60 phút)
% Tích hợp tốt với Kubernetes, Terraform; audit log đầy đủ
% Phù hợp service-to-service mTLS trong microservices

\subsubsection*{CFSSL (Cloudflare)}
% Ý chính: Golang, lightweight REST API; dùng nội bộ tại Cloudflare
% Ưu điểm: dễ nhúng vào CI/CD pipeline; hỗ trợ bundle, sign, verify

\subsubsection*{Dogtag / FreeIPA (Red Hat)}
% Ý chính: tích hợp LDAP (389-DS) + Kerberos + PKI trong một stack
% Phù hợp Linux enterprise; quản lý identity tập trung

% ----------------------------------------------------------
