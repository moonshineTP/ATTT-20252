% ============================================================
%  Chương VI – Các hệ thống PKI mã nguồn mở
% ============================================================
\chapter{Các hệ thống PKI mã nguồn mở}
\label{chap:opensource}

Hệ sinh thái PKI nguồn mở không tồn tại một "công cụ vạn năng" giải quyết mọi bài toán. Thay vào đó, nó phân mảnh thành các lớp giải pháp chuyên biệt, từ những bộ thư viện mật mã lõi cho đến các nền tảng quản trị danh tính quy mô doanh nghiệp. Việc lựa chọn công cụ phụ thuộc hoàn toàn vào mục tiêu vận hành. 

% ----------------------------------------------------------
\section{Tiêu chí phân loại}
\label{sec:6.1}

Dựa trên vai trò trong hệ thống, các công cụ PKI thường được chia thành năm nhóm chính:

\begin{description}
    \item[Cryptographic Toolkit:] Các tiện ích dòng lệnh và thư viện dùng để can thiệp trực tiếp vào cấu trúc mật mã (tạo khóa, ký CSR, parse chứng chỉ). OpenSSL là nền tảng tiêu biểu nhất.
    \item[Local Management:] Các phần mềm giao diện đồ họa hướng tới người dùng cá nhân hoặc môi trường lab (ví dụ: XCA~\cite{XCA}), lưu trữ cấu trúc CA gọn nhẹ trong cơ sở dữ liệu cục bộ.
    \item[ACME Client \& Public CA:] Nhóm công cụ sinh ra để giải quyết bài toán cấp phát chứng chỉ tự động trên diện rộng cho Web PKI, dẫn đầu bởi Let's Encrypt và Certbot~\cite{LetsEncryptDocs,CertbotDocs,RFC8555}.
    \item[CA Platform:] Các hệ thống cấp doanh nghiệp như EJBCA hay Dogtag~\cite{EJBCA,DogtagPKI}. Chúng quản lý vòng đời chứng chỉ thông qua hệ thống chính sách (profile), tích hợp audit, HSM và phân quyền quản trị nghiêm ngặt.
    \item[Internal PKI \& Secret Management:] Các cỗ máy phát hành chứng chỉ nội bộ vòng đời cực ngắn (short-lived), hoạt động hoàn toàn qua API để phục vụ xác thực giao tiếp giữa các vi dịch vụ (mTLS). HashiCorp Vault PKI là đại diện nổi bật~\cite{VaultPKI}.
\end{description}

Một công cụ xuất sắc khi học tập chưa chắc đã đáp ứng được yêu cầu audit của một CA thực tế. Ngược lại, việc triển khai một nền tảng enterprise chỉ để cấp vài chứng chỉ nội bộ là sự lãng phí về tài nguyên và chi phí vận hành.

% ----------------------------------------------------------
\section{OpenSSL}
\label{sec:6.2}

OpenSSL đóng vai trò "con dao Thụy Sĩ" trong hệ sinh thái mật mã. Dù không phải là một nền tảng CA hoàn chỉnh, nó cung cấp các module nền tảng để tương tác trực tiếp với cấu trúc X.509. 

Lệnh \texttt{openssl x509} cho phép bóc tách và hiển thị từng trường dữ liệu của chứng chỉ, chuyển đổi định dạng (PEM/DER) hoặc ký trực tiếp một CSR~\cite{OpenSSLX509}. Trong khi đó, lệnh \texttt{openssl verify} là công cụ đắc lực để gỡ lỗi quá trình dựng chuỗi tín nhiệm, cho phép chỉ định rõ trust anchor, đối chiếu hostname, email hay mục đích sử dụng~\cite{OpenSSLVerify}.

\begin{lstlisting}[language=bash, caption={Thao tác cấu trúc chứng chỉ bằng lệnh OpenSSL}, label={lst:openssl-pki}]
openssl x509 -in server.crt -text -noout
openssl verify -CAfile ca-chain.pem server.crt
openssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key -out server.crt
\end{lstlisting}

Điểm yếu chí mạng của OpenSSL khi đóng vai trò CA là sự vắng bóng của tầng quản trị. Mọi thao tác đều thủ công hoặc phải bọc qua các bash script. Nó không có khái niệm phân quyền quản trị, không có portal duyệt yêu cầu (RA), và thiếu vắng cơ chế ghi log tập trung phục vụ kiểm toán.

% ----------------------------------------------------------
\section{XCA}
\label{sec:6.3}

Để khắc phục rào cản dòng lệnh khô khan của OpenSSL, XCA mang đến một giao diện đồ họa trực quan để quản trị kho chứng chỉ cục bộ. Toàn bộ thông tin từ khóa riêng, CSR, chứng chỉ, CRL cho đến các template đều được tổ chức gọn gàng trong một cơ sở dữ liệu duy nhất (SQLite, MySQL hoặc PostgreSQL)~\cite{XCA}.

Sức mạnh của XCA nằm ở khả năng trực quan hóa. Người dùng có thể dễ dàng khởi tạo một Root CA, phân nhánh các Sub-CA và quan sát toàn bộ chuỗi tín nhiệm dưới dạng cây thư mục. Nó lý tưởng cho mục đích học tập hoặc quản lý mạng nội bộ quy mô nhỏ.

Tuy nhiên, giới hạn của XCA hoàn toàn tương đồng với OpenSSL: nó là công cụ cá nhân (local management). Nó thiếu API chuẩn hóa để tích hợp tự động, không có quy trình thẩm định (enrollment workflow) và không thể mở rộng thành một dịch vụ cấp phát chứng chỉ tập trung.

% ----------------------------------------------------------
\section{EJBCA}
\label{sec:6.4}

Khác biệt hoàn toàn với OpenSSL hay XCA, EJBCA là một nền tảng quản trị vòng đời chứng chỉ cấp doanh nghiệp thực thụ~\cite{EJBCA}. Trái tim của EJBCA không nằm ở thuật toán tạo khóa, mà ở hệ thống chính sách khắt khe kiểm soát việc phát hành.

Sự chặt chẽ này thể hiện qua việc phân tách hai lớp profile:
\begin{itemize}
    \item \textbf{Certificate Profile:} Định nghĩa "hình hài" của chứng chỉ được cấp ra (thuật toán, độ dài khóa, \texttt{KeyUsage}, \texttt{ExtendedKeyUsage}, tích hợp Certificate Transparency)~\cite{EJBCAProfiles}.
    \item \textbf{End Entity Profile:} Quy định "ai" được phép xin cấp chứng chỉ (cấu trúc Subject DN bắt buộc) và ràng buộc họ vào những Certificate Profile cụ thể nào~\cite{EJBCAEndEntityProfiles}.
\end{itemize}

Về mặt giao tiếp, EJBCA bộc lộ kiến trúc hiện đại qua hệ thống REST API đa dạng, bao phủ từ quản trị CA, duyệt yêu cầu đến trích xuất cấu hình~\cite{EJBCAREST}. Hơn thế nữa, nó hỗ trợ giao thức ACME đầy đủ các challenge phổ biến (\texttt{http-01}, \texttt{dns-01}, \texttt{tls-alpn-01}) để tích hợp trơn tru với các workload tự động~\cite{EJBCAACME}.

Trong khâu vận hành, EJBCA cung cấp sẵn OCSP Responder độc lập~\cite{EJBCAOCSP} và hỗ trợ kết nối trực tiếp với thiết bị mã hóa cứng (HSM). Việc bảo vệ khóa bí mật của Root CA trong HSM thay vì trên ổ cứng thông thường là lằn ranh phân biệt giữa một hệ thống thử nghiệm và một hạ tầng khóa công khai đạt chuẩn an toàn thông tin quốc gia.

% ----------------------------------------------------------
\section{Let’s Encrypt và Certbot}
\label{sec:6.5}

Nếu EJBCA giải quyết bài toán quản trị tập trung cho mạng nội bộ, thì Let’s Encrypt lại làm một cuộc cách mạng trên không gian Web PKI công cộng. Được vận hành bởi Internet Security Research Group, đây là một CA công cộng cấp phát chứng chỉ TLS hoàn toàn miễn phí~\cite{LetsEncryptDocs}.

Linh hồn của Let's Encrypt là giao thức ACME (RFC~8555) và công cụ Certbot~\cite{CertbotDocs}. Thay vì phải thao tác xin cấp thủ công, Certbot chứng minh quyền sở hữu tên miền với CA thông qua các bộ giải đố tự động (challenge):
\begin{itemize}
    \item \textbf{HTTP-01:} Tạo một file ngẫu nhiên tại \texttt{/.well-known/acme-challenge/} và phản hồi CA qua cổng 80~\cite{LetsEncryptChallenges}.
    \item \textbf{DNS-01:} Tạo bản ghi TXT tại \texttt{\_acme-challenge}. Đây là phương pháp duy nhất hỗ trợ cấp chứng chỉ wildcard~\cite{LetsEncryptChallenges}.
\end{itemize}

Lợi ích lớn nhất của Certbot là khả năng gia hạn im lặng (silent renewal). Lệnh \texttt{certbot renew} thường được lên lịch chạy ngầm qua cronjob, tự động xin cấp lại trước khi chứng chỉ hết hạn và kích hoạt các hook để tải lại cấu hình web server~\cite{CertbotDocs}. 

Tuy nhiên, quản trị viên cần hết sức chú ý đến các giới hạn tần suất (rate limit) của Let's Encrypt: việc kịch bản tự động bị lỗi và liên tục xin cấp lại một tên miền quá nhiều lần có thể dẫn tới việc bị khóa tài khoản hoặc khóa tên miền trong nhiều ngày~\cite{LetsEncryptRateLimits}.

% ----------------------------------------------------------
\section{Vault PKI}
\label{sec:6.6}

HashiCorp Vault PKI Secrets Engine đại diện cho triết lý thiết kế bảo mật vi dịch vụ (microservices). Thay vì xin cấp chứng chỉ thời hạn hàng năm từ một CA truyền thống, các dịch vụ gọi API đến Vault để lấy một chứng chỉ có vòng đời (TTL) chỉ tính bằng giờ, thậm chí bằng phút~\cite{VaultPKI}.

Quá trình cấp phát được ràng buộc bởi hệ thống Role, quy định chặt chẽ domain, subdomain và thời gian sống tối đa của chứng chỉ. Khi gọi endpoint \texttt{issue}, client nhận về trọn bộ chứng chỉ, khóa riêng và chuỗi CA để lập tức sử dụng~\cite{VaultPKIQuickStart}.

Triết lý TTL siêu ngắn mang lại ưu thế tuyệt đối về mặt vận hành: nó gần như triệt tiêu nhu cầu duy trì hệ thống thu hồi (CRL/OCSP) vốn rất nặng nề. Kẻ tấn công nếu đánh cắp được khóa phiên cũng chỉ có thể lợi dụng trong thời gian rất ngắn trước khi chứng chỉ tự bốc hơi. Mặc dù vậy, Vault PKI chỉ được các hệ thống nội bộ tin cậy; nó không thể dùng để cấp chứng chỉ TLS công cộng hướng tới thiết bị của người dùng cuối.

% ----------------------------------------------------------
\section{Dogtag và FreeIPA}
\label{sec:6.8}

Dogtag Certificate System là một CA platform mã nguồn mở cấp doanh nghiệp, cung cấp đầy đủ dịch vụ quản lý vòng đời chứng chỉ, profile, OCSP và SCEP~\cite{DogtagPKI}. Trong khi EJBCA mang tính đa nền tảng, Dogtag lại gắn kết chặt chẽ với hệ sinh thái Linux doanh nghiệp (đặc biệt là họ hệ điều hành Red Hat).

Sự kết hợp này phát huy tối đa sức mạnh khi Dogtag được nhúng vào làm lõi CA cho FreeIPA. Khác với CA truyền thống, FreeIPA là hệ thống quản trị định danh (Identity Management) toàn diện, bao bọc Kerberos, LDAP, DNS, user và host vào chung một miền duy nhất~\cite{FreeIPA}. Việc tích hợp sẵn Dogtag cho phép quản trị viên tự động hóa hoàn toàn luồng bảo mật: một máy chủ Linux khi gia nhập mạng nội bộ sẽ tự động nhận danh tính Kerberos, đồng thời song song được cấp chứng chỉ X.509 để mã hóa kênh truyền nội bộ, tất cả được điều phối nhịp nhàng từ một điểm tập trung.

% ----------------------------------------------------------
\section{So sánh hệ sinh thái}
\label{sec:6.9}

\begin{table}[htbp]
    \centering
    \small
    \renewcommand{\arraystretch}{1.4}
    \caption{So sánh một số công cụ và hệ thống PKI mã nguồn mở}
    \label{tab:opensource-pki-tools}
    \begin{tabular}{@{} >{\raggedright\arraybackslash}p{0.14\textwidth} >{\raggedright\arraybackslash}p{0.16\textwidth} >{\raggedright\arraybackslash}p{0.20\textwidth} >{\raggedright\arraybackslash}p{0.20\textwidth} >{\raggedright\arraybackslash}p{0.20\textwidth} @{}}
        \toprule
        \textbf{Công cụ} & \textbf{Phân loại} & \textbf{Mục tiêu cốt lõi} & \textbf{Đặc điểm vận hành} & \textbf{Hạn chế} \\
        \midrule
        \textbf{OpenSSL} & Thư viện / Toolkit & Thao tác khóa, ký CSR, parse chứng chỉ & Giao diện CLI, cấu hình CA cơ bản & Không quản lý tập trung, thiếu API/RA. \\
        \textbf{XCA} & Quản trị cục bộ & Quản lý CA cục bộ bằng giao diện đồ họa & Lưu trữ bằng CSDL cục bộ (SQLite) & Không có API, không tự động hóa cấp phát. \\
        \textbf{EJBCA} & Enterprise CA & Quản trị toàn bộ vòng đời PKI nội bộ & REST/ACME API, hỗ trợ OCSP/CRL và HSM & Cấu hình chính sách và vận hành phức tạp. \\
        \textbf{Let's Encrypt / Certbot} & Public ACME CA & Cấp và gia hạn tự động chứng chỉ Web TLS & Xác minh DNS/HTTP, tự động hóa cronjob & Bị áp rate limit, chỉ cấp chứng chỉ DV. \\
        \textbf{Vault PKI} & Internal PKI & Cấp chứng chỉ ngắn hạn cho Microservices & Thuần API, không cần CRL nhờ TTL ngắn & Chỉ dùng nội bộ, không thay Public CA. \\
        \textbf{Dogtag / FreeIPA} & Enterprise Identity & Tích hợp CA với hệ thống định danh & Tích hợp chặt chẽ với LDAP/Kerberos & Phụ thuộc mạnh vào hệ sinh thái Linux. \\
        \bottomrule
    \end{tabular}
\end{table}

